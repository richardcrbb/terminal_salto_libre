import 'package:flutter/material.dart';

class GlidePathPage extends StatelessWidget {
  const GlidePathPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}