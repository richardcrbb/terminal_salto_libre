import 'package:flutter/material.dart';
//import 'package:google_maps_flutter/google_maps_flutter.dart';

class GlidePathPage extends StatelessWidget {
  const GlidePathPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      /*appBar: AppBar(title: const Text("Recorrido bajo cúpula")),
      body: GoogleMap(
        initialCameraPosition: CameraPosition(
          target: recorrido.first,
          zoom: 15,
        ),
        polylines: {
          Polyline(
            polylineId: const PolylineId("recorrido"),
            points: recorrido,
            color: Colors.blue,
            width: 5,
          ),
        },
        markers: {
          Marker(
            markerId: const MarkerId("inicio"),
            position: recorrido.first,
            infoWindow: const InfoWindow(title: "Inicio"),
          ),
          Marker(
            markerId: const MarkerId("fin"),
            position: recorrido.last,
            infoWindow: const InfoWindow(title: "Fin"),
          ),
        },
      ),*/
    );
  }
}